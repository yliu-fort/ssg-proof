# Round 18 — brief

Supplement to `conjecture.md` (which stands unchanged) and to the user's four
additions of round 17 (`rounds/round17/BRIEF.md`, which stand: the novelty
pre-check against the full inventory, the four imports only, object-changing
routes dominant with the tightened verdict scale, at most one paper audit).
Set by the root agent on 2026-09-04 08:40 UTC on the user's instruction
"查看 readme 和 conjecture.md, rounds/round17/brief.md, 检查仓库当前情况,
然后开始工作, 注意只允许启动 opus agents": every agent on Opus 5, launched
at once.

## Where round 17 left the search (rounds/round17/README.md)

The pivot is unmoved: no superpolynomial all-switches family, no
realisation of the third blow-up level, and the one shape that reached its
outer half is proved unable to go further (`cor:pinned-no-doubling`).
Round 17's routes named, as their own next steps, the questions below;
every route of this round takes one of them and attaches it to an object
the paper does not yet own.

## The routes (all Opus 5, two audits each)

| key | object | the deliverable that counts |
| --- | --- | --- |
| `drive-line` | the drive line of a driven block (the step function from the drive value to the orientation the block presents), with the escape shape of `rem:pinned-escape` and $B^3$ as the test case | the fence/single-flip theorem and a lower bound on what a doubling translate costs on a drive line; the escape shape's level theorem, decided at $m=3$ |
| `one-player-envelope` | the upper envelope of monotone rational functions that the one-player stopping-probability path is | a proved envelope bound; the one-player path superpolynomial or bounded; whether the tent itinerary can drive an all-switches run |
| `eval-decision` | the decision version of the evaluation-query model on nondegenerate one-skeleton families; the consistent set $K(D)$ | a proved superconstant lower bound for deciding $\val(v_0)\ge\tfrac12$ from evaluations, or a proved $O(1)$ bound; the status of the rank-deficient relaxation |
| `convex-class` | the handicap-zero class $\mathcal R$ ($B\succeq0$) and the convex complementarity sum over $Q(G)$ | a first-principles polynomial algorithm on $\mathcal R$, or a two-player member outside the eight classes, or the convex cutting plane as a seventh mechanism with its barrier |
| `weakest-oracle` | the hierarchy of partial-information fragments of the value order under polynomial-time reduction | which fragments (any strict comparison; the $\varepsilon$-order; the order on a set) are target-equivalent and which are polynomial, each by a proved reduction or algorithm |
| `beyond-holt-klee` | the LP orientations of occupancy polytopes $X(G)$ as a class of cube orientations | a necessary condition beyond Holt--Klee with a Holt--Klee orientation violating it; $h^*_1(5)\in\{10,11\}$ decided |
| `rlt-two` | the level-two Sherali--Adams lift over the choice variables | an engineered level-two gap verified from a game, or an exactness theorem on a named class |

## The paper audit (one agent)

The 1713 lines added to `frontier.tex` in round 17 (batches A--I,
`git diff 7fa45a3..HEAD`), which no one has read as paper text; secondary
targets, if time allows, the backlog round 17's audit did not check
(the escape exponent $d(M_n)=n+1$; `prop:hk-doubling-measured`(d),(e);
`prop:oneplayer-census-small`'s total; `prop:cv-measured`; `prop:q16`;
`prop:bias-witnesses`(b)--(d); `lem:max-tree`'s instance; `prop:zero-ties`).

## What changed in the standing rule

The digest handed to every agent now says what round 17 proved: the
own-successor readings and the non-strict pair test are INCOMPARABLE, and a
stall claim needs all three silent (own-successor clauses (i) and (ii), and
$\operatorname{Sep}(v^{(0)},v^{(1)})\le0$ / $\operatorname{Sep}(v^{(1)},v^{(0)})\le0$),
all $Z$-seeded, at a value-distinguishing vertex. Everything else is
unchanged: exact rational arithmetic for every claim, the known-traps list,
no writing into the repository, no background jobs left behind, structured
output plus paste-ready LaTeX, the root agent verifying every load-bearing
claim before integration and applying a route only after both its audits.
